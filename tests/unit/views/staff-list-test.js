import { test, moduleFor } from 'ember-qunit';

moduleFor('view:staff-list', 'StaffListView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
