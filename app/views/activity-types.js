import Ember from 'ember';

export default Ember.View.extend({
  didRenderElement: function() {
    /*
    this.$('#activity-types-table').dataTable({
      "pageLength": 5,
      "process": true
    });
     */
  }
});
